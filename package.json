{
  "name": "magic-rpg",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist -t",
    "art": "python scripts/process_art.py"
  },
  "dependencies": {
    "phaser": "^4.1.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^8.0.14",
    "vite-plugin-pwa": "^1.3.0"
  }
}

# 開發指南 · 你和我怎麼分工

這份文件設計我們的合作方式，目標是：**你能自己快速改的東西就自己改（即時生效），
需要動到「程式系統」的才找我**，這樣最快。

---

## 🟢 你負責的（不用寫程式，存檔就即時生效）

這些都是「**內容與數值**」，改起來像填表格。

### 開始之前：打開「即時預覽」
在專案資料夾執行一次：
```
npm run dev
```
然後讓那個視窗一直開著、瀏覽器開著 http://localhost:5173/ 。
**之後你每次改檔案存檔，瀏覽器會自動更新**——這就是最快的循環，完全不用等我。

### 你可以直接改的檔案

| 想做的事 | 改哪個檔 | 怎麼改 |
|---------|---------|--------|
| 怪物變強／變弱 | `src/data/enemies.js` | 改 `hp`、`atk`、`exp` 等數字 |
| 新增道具 | `src/data/items.js` | 複製一個區塊，改 id、名稱、效果 |
| 改技能傷害／SP | `src/data/skills.js` | 改 `power`、`sp`、`amount` |
| 寫／改 NPC 對話 | `src/data/planets.js` | 改該 NPC 的 `lines: [ ... ]` 字串 |
| 在星球上加 NPC | `src/data/planets.js` | 在 `npcs` 陣列加一筆 |
| 移動怪物／道具位置 | `src/data/planets.js` | 改 `x`、`y` 座標 |
| 加一顆新星球 | `src/data/planets.js` | 複製整個星球區塊改內容（見下） |
| 換美術 | `art-source/` 資料夾 | 丟圖進去（看那裡的 README） |

### 範例：寫一句新對話
在 `planets.js` 找到某個 NPC：
```js
lines: [
  "村民：歡迎來到這個世界。",
  "村民：這是我新加的一句話！",   // ← 直接加一行，存檔就生效
],
```

### 範例：加一顆新星球
複製一整個星球區塊（例如 `verdant: { ... }`），貼上後改：
```js
desert: {                          // ← 新 id（不能跟別的重複）
  name: "黃沙星",
  size: { w: 1280, h: 960 },
  spawn: { x: 640, y: 850 },
  ground: 0xC2A36A, grid: 0xA8884A, accent: 0xFFD27F,  // ← 配色
  decorColors: [0xA8884A, 0xC2A36A, 0xE6C98A],
  npcs: [ /* ... */ ],
  enemies: [ { x: 400, y: 600, id: "scorpion" } ],
  items: [],
  portals: [ { x: 640, y: 110, to: "station", label: "返回太空站" } ],
},
```
再到 `station`（太空站）的 `portals` 加一個傳送台指向它：
```js
{ x: 600, y: 300, to: "desert", label: "黃沙星" },
```
存檔 → 太空站就多一個傳送台，走過去就到新星球。

> 顏色用 `0xRRGGBB` 十六進位（跟網頁色碼一樣，只是開頭從 `#` 換成 `0x`）。

---

## 🔵 找我做的（需要動到程式系統）

- 新「機制／系統」：商店、存檔、裝備、隊友、Boss 特殊技…
- 修 bug、畫面跑版、效能問題
- 把美術接進遊戲（素材管線）
- 任何你不確定、或改了會壞掉的東西

直接跟我說「我要 X」即可。

---

## 🎨 換美術的流程

1. 把圖丟進 `art-source/`（命名看那裡的 README）
2. 跟我說一聲（或之後用 `npm run art` 自動處理）
3. 圖會處理好並接進遊戲，色塊就被換成真圖

（`npm run art` 指令我會在你給第一批圖後幫你建好。）

---

## 🚀 發佈到線上

改好、想更新線上版本：
```
npm run deploy
```
幾十秒後重新整理 https://nicktim791113.github.io/magic-rpg/ 就是新版。

---

## ⚠️ 改壞了怎麼辦？

- 如果存檔後瀏覽器出現**紅色錯誤畫面**，多半是**少了逗號、引號或括號**。
  - 按 `Ctrl + Z` 還原到上一步，再小心改一次。
  - 紅字通常會寫第幾行出錯。
- 真的卡住，把錯誤訊息貼給我，我幫你修。
- 建議：一次只改一個地方、存檔看結果，比較好抓問題。
